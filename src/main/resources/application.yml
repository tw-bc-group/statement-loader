spring:
  application:
    name: statement-loader
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL8Dialect
    open-in-view: false
    properties:
      hibernates:
        jdbc:
          time_zone: UTC
  output:
    ansi:
      enabled: always
  profiles:
    active:
